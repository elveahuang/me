'use client';

import { SwitchProps } from '@heroui/react';
import { FC, JSX } from 'react';

export interface ThemeSwitchProps {
    className?: string;
    classNames?: SwitchProps[];
}

export const ThemeSwitch: FC<ThemeSwitchProps> = (): JSX.Element => {
    return <div></div>;
};
