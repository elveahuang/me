import { FC } from 'react';
import 'video.js/dist/video-js.css';

export type QEditorProps = {
    options?: any;
    onReady?: void;
};

export const QEditor: FC<QEditorProps> = (props: QEditorProps) => {
    return <div></div>;
};

export default QEditor;
